# Windows 動作テスト手順

このブランチ (`claude/windows-test-VHFdI`) には、音声入力レビューで実装した
**3つの変更** が入っています。Windows 実機での確認用です。

| # | 変更内容 | テストできるモード |
|---|----------|--------------------|
| 1 | 貼り付けレース修正(直前にコピーした物が貼られるバグ) | 開発モード / パッケージ版 どちらでも可 |
| 2 | 自動アップデートの手動更新UI | **パッケージ版のみ**(開発モードでは無効) |
| 3 | Win+V クリップボード履歴からの除外 | 開発モード / パッケージ版 どちらでも可 |

---

## 1. 準備

### 必要なもの
- Windows 10 / 11(x64)
- Node.js 22 以上 — <https://nodejs.org/> から Chrome でダウンロードしてインストール
- Git(任意。ZIP ダウンロードでも可)

### コードの取得

**方法A: git clone(推奨)**
```powershell
git clone -b claude/windows-test-VHFdI https://github.com/mackatwentytsuru/windvoice.git
cd windvoice
```

**方法B: Chrome で ZIP ダウンロード**
1. Chrome で <https://github.com/mackatwentytsuru/windvoice/tree/claude/windows-test-VHFdI> を開く
2. 緑の「Code」ボタン → 「Download ZIP」
3. 展開して、そのフォルダで PowerShell を開く

### 依存関係のインストール
```powershell
npm install
```
`uiohook-napi` / `keytar` / `sendinput` / `koffi` などのネイティブモジュールが
Electron 用に rebuild されます。エラーが出る場合は Visual Studio Build Tools が
必要なことがあります。

---

## 2. 起動方法

### 開発モード(テスト1・3 用)
```powershell
npm run dev
```
- 機能 **1(貼り付け)** と **3(Win+V 除外)** はこのモードで確認できます。
- 機能 **2(自動アップデート)** は `app.isPackaged === false` の開発モードでは
  無効化されるため、確認できません。

### パッケージ版(テスト1・2・3 すべて)
```powershell
npm run package:win
```
- `release\<version>\WindVoice-Setup-<version>-x64.exe` が生成されます。
- これを実行してインストール → スタートメニューから WindVoice を起動。
- 未署名ビルドのため、SmartScreen は「詳細情報」→「実行」で通します。

### 初回セットアップ
設定ウィンドウで OpenAI API キーを入力してください(音声入力に必須)。

---

## 3. テスト項目

### A. 貼り付けレース修正(機能1)

**目的**: 「一つ前にコピーした内容が貼り付けられてしまう」バグが直っているか。

**手順**:
1. 設定 → 一般 →「貼り付けの確実性」が **標準(推奨)** になっているか確認。
2. 何か分かりやすい文字列をコピーしておく(例: `OLDCLIP123`)。
3. 貼り付け先にフォーカスを移す(下記のいくつかで試す)。
4. ホットキーを押して音声入力 → 何か喋る → ホットキーを離す。
5. **期待**: 喋った内容が貼り付く。`OLDCLIP123` が出たら **NG**。

**試す貼り付け先**(特に遅い環境が重要):
- [ ] Windows Terminal
- [ ] WSL(Windows Terminal 内の Ubuntu など)
- [ ] PowerShell / コマンドプロンプト
- [ ] **Chrome**(アドレスバー・検索ボックス・テキストエリア)
- [ ] メモ帳
- [ ] VS Code

**NG だった場合**:
- 設定の「貼り付けの確実性」を **確実(ターミナル/リモート向け)** に変更して
  再テスト。これで直れば、その環境向けに `safe` プロファイルが必要という結果です。
- 逆に「速い(低遅延)」でわざと再現するかも確認すると切り分けに役立ちます。

---

### B. Win+V クリップボード履歴からの除外(機能3)

**目的**: 音声入力したテキストが Windows のクリップボード履歴(`Win + V`)に
残らないこと。かつ、貼り付け自体は成功すること。

**事前確認**: Windows の設定で「クリップボードの履歴」がオンになっていること
(設定 → システム → クリップボード)。オフだと履歴自体が記録されません。

**手順**:
1. WindVoice 設定 → 一般 →「クリップボード履歴に残さない (Windows)」が
   **オン** になっているか確認。
2. 適当なテキストを手動でコピーする(例: `手動コピーA`)。
3. 音声入力を1回行う(できれば複数単語)。
4. `Win + V` を押してクリップボード履歴を開く。
5. **期待**:
   - 音声入力したテキストが履歴に **出てこない**。
   - `手動コピーA` は履歴に残っている。
   - 貼り付け先には、ちゃんと音声入力のテキストが入っている(← 重要)。
6. **対比テスト**: 設定をオフにして手順2〜4を繰り返すと、今度は音声入力した
   テキストが履歴に出るはず。オン/オフで挙動が変わることを確認。

**注意 — 安全設計について**:
この機能は Windows ネイティブAPI(koffi 経由)を使いますが、**失敗しても
貼り付けは壊れない** よう設計しています。最悪のケースは「履歴に残ってしまう」
だけです。もし履歴に残ってしまう場合は、デバッグログ(下記)で次のような行が
出ていないか確認してください:
- `koffi clipboard FFI unavailable: ...` … koffi が読み込めていない
- `excluded clipboard write failed: ...` … Win32 呼び出しが失敗した

ストリーミング挿入(設定の「ストリーミング挿入」)をオンにしている場合も、
チャンクごとに履歴へ断片が残らないことを確認してください。

---

### C. 自動アップデートの手動更新UI(機能2)— パッケージ版必須

**目的**: 「更新の確認 → ダウンロード → 再起動」の手動フローが動くこと。

**手順**:
1. パッケージ版をインストールして起動。
2. 設定 → 一般 →「更新を確認」ボタンを押す。
3. **期待(最新の場合)**: ボタン横に「最新版です」と表示。
4. **期待(新しい Release がある場合)**:
   - 設定ウィンドウ上部にバナーが出る:「新しいバージョンが利用可能 (vX.Y.Z)」
   - 「ダウンロード」ボタン → 押すと「ダウンロード中 NN%」表示
   - 完了すると「ダウンロード完了。再起動で適用。」+「今すぐ再起動」ボタン
   - 「今すぐ再起動」で更新が適用される
5. エラー時はバナーが赤く「アップデートエラー: ...」と表示される。

**設計メモ**: 未署名ビルドのため、更新は **自動ではダウンロードしません**。
必ずユーザーがボタンを押したときだけダウンロード・インストールが進みます。

---

## 4. デバッグログの取り方

問題が起きたとき、原因切り分けのためにログを取ってください。

### 開発モード
PowerShell で環境変数をセットしてから起動:
```powershell
$env:WINDVOICE_DEBUG_DICTATION = "1"
$env:WINDVOICE_DEBUG_HOTKEY = "1"
npm run dev
```
コンソール(stderr)に `[dictation]` `[hotkey]` などのログが流れます。

### パッケージ版
インストール先の実行ファイルを、環境変数付きでコマンドラインから起動:
```powershell
$env:WINDVOICE_DEBUG_DICTATION = "1"
& "$env:LOCALAPPDATA\Programs\WindVoice\WindVoice.exe"
```
(インストール先はインストーラの設定により異なります)

---

## 5. 結果の報告

各項目について **OK / NG** と、NG の場合は以下を PR #48 にコメントしてください:
- 機能 A: NG だった貼り付け先アプリ名、試した「貼り付けの確実性」プロファイル
- 機能 B: 履歴に残ったか、貼り付けは成功したか、debug ログの該当行
- 機能 C: バナーの表示内容、エラーメッセージ

関連 PR: <https://github.com/mackatwentytsuru/windvoice/pull/48>
