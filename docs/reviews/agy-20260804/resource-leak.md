^D[重大度: 重大] [src/main/linux/portalSidecar.ts:161-169, 473, 513, 528](file:///Users/yukitsuruoka/Development/projects/windvoice/src/main/linux/portalSidecar.ts#L161-L169) / `r.tainted` 発生時の `this.restart()` 呼び出しによる `MAX_RESPAWNS` バジェット無効化と Mutter セッション無限再生成ループ / `setSelection` ([line 513](file:///Users/yukitsuruoka/Development/projects/windvoice/src/main/linux/portalSidecar.ts#L513))、`pasteText` ([line 473](file:///Users/yukitsuruoka/Development/projects/windvoice/src/main/linux/portalSidecar.ts#L473))、`keyPaste` ([line 528](file:///Users/yukitsuruoka/Development/projects/windvoice/src/main/linux/portalSidecar.ts#L528)) では `r.tainted === true`（`SelectionOwnerTimeout` 等の発生時）を受け取ると `this.restart()` を呼び出しているが、`restart()` 内 ([line 161-169](file:///Users/yukitsuruoka/Development/projects/windvoice/src/main/linux/portalSidecar.ts#L161-L169)) で `this.respawns = 0` にリセットして `this.start()` を行う。再生成上限である `MAX_RESPAWNS` (5) の判定は `requestRespawn()` ([line 291](file:///Users/yukitsuruoka/Development/projects/windvoice/src/main/linux/portalSidecar.ts#L291)) にしか存在しないため、`SelectionOwnerTimeout` が連続発生した場合に `restart()` が呼ばれるたびにバジェットがリセットされ、無制限に Sidecar 再生成（および Mutter/portal への `CreateSession` 要求）が繰り返される。これにより、Mutter 側に RemoteDesktop セッションが蓄積し、実障害である Session Inhibit をアプリ側で悪化させてしまう。 / `tainted` によるセッション再生成時は無条件の `this.restart()` ではなく `requestRespawn()` を通すか、連続エラー時には `respawns` カウントを保持・インクリメントして `MAX_RESPAWNS` 超過時に `onUnavailable` を通知して再生成ループを停止させる。

[重大度: 中] [resources/native/portal-remote.py:446-448, 455-464](file:///Users/yukitsuruoka/Development/projects/windvoice/resources/native/portal-remote.py#L446-L448) / `setup_session` のセットアップ途中失敗（`SelectDevices` / `Start` のエラー・拒否）における D-Bus Portal セッションの Close 漏れ / `CreateSession` 成功時 ([line 400](file:///Users/yukitsuruoka/Development/projects/windvoice/resources/native/portal-remote.py#L400)) に `state['session'] = session` が保持された後、`SelectDevices` 失敗時 ([line 446-448](file:///Users/yukitsuruoka/Development/projects/windvoice/resources/native/portal-remote.py#L446-L448)) および `Start` 失敗時 ([line 455-464](file:///Users/yukitsuruoka/Development/projects/windvoice/resources/native/portal-remote.py#L455-L464)) において、作成済み `session` に対して `Close` メソッドを呼ばずに `return False` している。`stdin_worker` ([line 906-909](file:///Users/yukitsuruoka/Development/projects/windvoice/resources/native/portal-remote.py#L906-L909)) は `setup_session_guarded()` が `False` を返すと親プロセスからの SIGTERM/kill を待つため `time.sleep(3600)` で生存する。このため、`kill` が届くまでの間、Mutter/portal 側に中途半端な RemoteDesktop セッションが明示破棄されず解放待ちのまま残り続ける。 (対照的に [line 498-505](file:///Users/yukitsuruoka/Development/projects/windvoice/resources/native/portal-remote.py#L498-L505) の stale token retry 時には明示的に `Close` を呼んでいる)。 / `SelectDevices` や `Start` が非0コードで失敗した際も `return False` 前に `session` に対して D-Bus `Close` を呼び出す（または `try...finally` で保護する）ことで、Mutter/portal 側のセッションリソースを即座に解放する。

[確認済み] [resources/native/portal_clipboard.py:40-49](file:///Users/yukitsuruoka/Development/projects/windvoice/resources/native/portal_clipboard.py#L40-L49) (`OwnerChangeBarrier.wait_until_owned`) / `threading.Condition.wait(remaining)` を使用し、`time.monotonic()` による相対タイムアウト計算で待機を行っているため、`SelectionOwnerChanged` シグナルが来ない・遅延した場合でもスレッドが永続ブロック・リークすることはなく、指定タイムアウト（2.0秒）経過後に確実に `False` を返して終了することを確認した。

[確認済み] [resources/native/portal-remote.py:243-246, 585-613](file:///Users/yukitsuruoka/Development/projects/windvoice/resources/native/portal-remote.py#L243-L246) (`serve_selection_transfer` / `read_selection_snapshot`) / D-Bus 経由で取得した UNIX file descriptor (`fd`) は、正常処理・タイムアウト・例外発生の全経路において `finally: os.close(fd)` ブロックにより確実にクローズされており、ファイルディスクリプタのリークが発生しないことを確認した。

[確認済み] [resources/native/portal-remote.py:200-208](file:///Users/yukitsuruoka/Development/projects/windvoice/resources/native/portal-remote.py#L200-L208) (`portal_request`) / D-Bus の Request/Response 処理における `bus.signal_subscribe` は `try...finally` ブロックで `bus.signal_unsubscribe(sub)` が必ず実行されるため、タイムアウトや例外発生時にも D-Bus シグナルハンドラが漏洩・蓄積しないことを確認した。

REVIEW_DONE
