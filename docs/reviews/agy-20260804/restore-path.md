^D[確認済み] resources/native/portal-remote.py:794-823 / 貼り付け処理自体が成功（`injected=True` かつ `selection_read=True`）していれば、その後の `restore` 処理で `SelectionOwnerTimeout` が発生しても貼り付け結果 `ok` は `True` となり（`restored=False`, `tainted=True`）、クリップボード復元の失敗によって貼り付け自体の成功判定が打ち消されない整合性が保たれている。

[確認済み] resources/native/portal_clipboard.py:11-49 / `OwnerChangeBarrier` が `checkpoint()` と `_sequence` の増分チェックを用いることで、前回の claim/restore により既に `session_is_owner` が `True` の状態であっても、今回の `SetSelection` 呼び出し以降に発生した新たな `SelectionOwnerChanged` シグナルのみを待機・受領する設計になっており、過去の所有権状態による誤認が防がれている。

[確認済み] src/main/linux/portalSidecar.ts:468-474 / restore 処理のタイムアウト等で sidecar から `tainted: true` (`sessionReset`) が返却された際、`portalSidecar` が `this.restart()` を呼び出してセッションを即座に再構築するため、未反映の `SetSelection` が遅れて適用された場合でも後続のセッションへ汚染が及ばない安全策が機能している。

[重大度: 中] resources/native/portal-remote.py:905-923 / src/main/linux/portalSidecar.ts:468-474, 255-271 / 1回目の dictation の restore 処理が owner 待ちでタイムアウト（`SelectionOwnerTimeout` で 2 秒待機）して `tainted=True` を返した際、その 2 秒間の待機中に送信された 2 回目の dictation リクエストが Python 側の stdin にキューイングされていると、1 回目の応答完了直後に Python プロセスが 2 回目のリクエストを処理し始めるが、Node.js 側は 1 回目の `tainted=True` 受信直後に `this.restart()` を実行して Python プロセスを SIGTERM キルし、2 回目の dictation を `uncertain: true` で即時失敗させる競合が発生する。 / 【根拠】 `portal-remote.py:905-923` の `stdin_worker` は単一スレッドの同期ループであり、1 回目の `handle_paste` 完了後すぐに stdin バッファにある次の JSON 行を読破して `handle_paste`（`set_selection` 等）を開始する。一方、Node.js 側の `portalSidecar.ts:471-473` は `r.tainted === true` を検知すると同期的に `this.restart()` を呼び出し、`teardownChild` (`portalSidecar.ts:255-271`) で Node 側 `pending` に入っている 2 回目の dictation Promise を `{ ok: false, error: 'sidecar restarting', uncertain: true }` に即座に拒否・解決させた上で `child.kill()` を送出する。これにより、連投された 2 回目の dictation は打ち切られペースト失敗（`uncertain: true`）となる。 / 【修正案】 `portal-remote.py` で `tainted` となるエラーが発生した場合は、stdin に溜まった後続のリクエストを消化せず `os._exit(1)` 等で自ら即座に終了してプロセス再生成に委ねるか、または Node.js 側で `restart()` を呼ぶ際に `pending` なリクエストが存在する場合のセッション再構築完了待機・自動再試行（retry）機構を導入する。

[重大度: 軽微] src/main/linux/portalSidecar.ts:440 / `pasteText()` における Node.js 側の Promise タイムアウト予算 `budget` の計算式が `settleMs + restoreMs + WAYLAND_PASTE_VERIFICATION_MS + 15_000` となっており、`set_selection()` 内部で発生し得る owner 待機時間（claim 時と restore 時で最大 2.0 秒 × 2 = 4.0 秒）の定数値が計算式で明示されていない。 / 【根拠】 `portal-remote.py:765, 802` では `set_selection()` が claim と restore で計 2 回呼び出され、それぞれ `SELECTION_OWNER_TIMEOUT_S` (2.0秒) 待機する可能性がある。現在の固定値 `15_000` ms のマージンによって実効上タイムアウト切れは防がれているものの、`WAYLAND_PASTE_VERIFICATION_MS` や `settleMs` などの引数と異なり、owner 待ちタイムアウト上限が明示的な計算要素として組み込まれていない。 / 【修正案】 `budget` の算式に Python 側の `SELECTION_OWNER_TIMEOUT_S * 2` (4000ms) を明示的な変数/定数として含めるか、コメントで owner 待ち時間上限が固定マージンに含まれる旨を明記する。

REVIEW_DONE
